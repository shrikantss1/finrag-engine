# Setup

This document will cover environment setup, dependency installation, and local configuration.

## Prerequisites

- Python 3.10+
- Docker and Docker Compose
- Access to a vector database provider or local instance

## Steps

1. Create a virtual environment.
2. Install dependencies with `pip install -r requirements.txt`.
3. Configure environment variables in `config/`.
4. Start dependencies via `docker-compose.yml`.
